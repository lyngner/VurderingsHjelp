
# Oppdatering v8.5.3 - Nested Bracket Fix

Denne oppdateringen løser visningsfeil der tekst "sølte ut" av tolkning-boksene i Kontroll-steget.

### 🛡️ Endringer:

1.  **Balansert Parsing (Nested Brackets)**:
    *   **Problemet:** Tidligere brukte systemet en enkel Regex (`.*?`) for å finne innholdet i `[BILDEVEDLEGG: ...]`. Hvis innholdet hadde egne klammeparenteser (f.eks. `[def f(x):]`), stoppet parsingen ved den første lukke-klammen `]`. Dette førte til at resten av teksten havnet utenfor boksen.
    *   **Løsningen:** Implementert en ny parser (`splitByImagesBalanced`) som teller åpne og lukkede klammer. Den vet nå at hvis det åpnes en ny `[`, må det komme en `]` som tilhører den, *før* hovedblokken kan lukkes.
    *   **Resultat:** Komplekse figurbeskrivelser og kodeblokker med brackets holdes nå samlet inni den grå boksen.

### Teknisk:
*   Oppdatert `LatexRenderer` i `SharedUI.tsx` med ny tilstandsmaskin-parser.
*   Systemversjon: **v8.5.3**.
