
# Oppdatering v8.0.32 - Smart Anti-Stutter Filter

Denne oppdateringen løser "stamme-problemet" (Stuttering Bug) der Gemini Flash feilaktig leser ensifrede oppgavenummer som to-sifrede (f.eks. "11a" i stedet for "1a").

### 🛡️ Kritiske endringer:

1.  **Aggressiv Stutter-Fix (I Filteret)**:
    *   Lagt inn en logikk i `filterTasksAgainstRubric` som sjekker om et oppgavenummer består av repeterende sifre (f.eks. "11", "22", "33").
    *   Hvis et slikt nummer dukker opp fra OCR, og det **ikke** finnes i rettemanualen, sjekker systemet om den ensifrede versjonen ("1", "2", "3") finnes.
    *   Hvis ja, blir oppgaven automatisk korrigert ("11a" -> "1a") før den lagres.

2.  **Hvorfor var dette nødvendig?**
    *   Fordi Flash-modellen "stammer" når den prosesserer raskt uten tenke-budsjett. Dette filteret renser dataene slik at badgene (oppgavemerkene) matcher fasiten og dukker opp i grensesnittet igjen.

### Teknisk:
*   Oppdatert `filterTasksAgainstRubric` i `geminiService.ts` med regex-sjekk `^(\d)\1+$`.
*   Systemversjon: **v8.0.32**.
