
# Oppdatering v9.1.16 - The Great Escape (JSON Fix)

Denne oppdateringen løser "SyntaxError: Bad escaped character" problemene som forårsaket at innlastingskøen stoppet opp.

### 🛡️ Kritiske Feilrettinger:

1.  **Quadruple Escape Logic**:
    *   **Problemet:** Min tidligere fiks for "bad escapes" (som `\s` og `\a`) la bare på én ekstra backslash. Dette resulterte i JSON-strenger som `\\s`, som `JSON.parse` fortsatt tolker som ulovlige escape-sekvenser (fordi det blir `\s` i minnet).
    *   **Løsningen:** Oppdatert `cleanJson` til å bruke **8 backslashes** i erstatningsstrengen (`\\\\\\\\`).
    *   **Matematikken:** 8 i kildekode -> 4 i strengen -> 2 i JSON-kilden -> 1 bokstavelig backslash i minnet. Dette gjør at `\sigma` blir korrekt lest som teksten `\sigma` uten å krasje parseren.

2.  **Queue Deadlock Fix (fra v9.1.15)**:
    *   Inkludert fiksen som hindrer at en enkelt fil-feil stopper hele køen. Systemet vil nå markere filen som "Feilet" og gå videre til neste.

### Teknisk:
*   Oppdatert regex-erstatninger i `geminiService.ts`.
*   Systemversjon: **v9.1.16**.
