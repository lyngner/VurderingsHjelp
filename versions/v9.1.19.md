
# Oppdatering v9.1.19 - The Negative Lookahead Fix

Denne oppdateringen løser "SyntaxError: Bad escaped character" problemene en gang for alle ved å bruke en matematisk sikker regex-metode.

### 🛡️ Kritiske Feilrettinger:

1.  **Negative Lookahead JSON Sweep**:
    *   **Problemet:** Tidligere forsøk på å fikse JSON-escaping feilet i kanttilfeller fordi logikken prøvde å være "smart" ved å beskytte noen tokens og erstatte andre.
    *   **Løsningen:** Vi bruker nå en enkelt Regex med "Negative Lookahead": `/\\(?!(?:["\\/nrt]|u[0-9a-fA-F]{4}))/g`.
    *   **Hva gjør den?** Den finner alle backslasher `\` som **IKKE** etterfølges av en gyldig JSON-escape-kode (`"`, `\`, `/`, `n`, `r`, `t` eller `uXXXX`).
    *   Alle slike "uautoriserte" backslasher (som `\s` i `\sigma` eller `\b` i `\begin`) blir erstattet med `\\` (dobbel backslash).
    *   Dette gjør at teksten blir lovlig JSON-streng, uten å ødelegge gyldige linjeskift.

### Teknisk:
*   Total ombygging av `cleanJson` i `geminiService.ts` til å bruke `replace` med negative lookahead.
*   Systemversjon: **v9.1.19**.
