
# Oppdatering v6.6.8 - Strict Orientation & Split

Denne oppdateringen innfører en radikalt forenklet og streng pipeline for å garantere korrekt håndtering av A3-skanninger og hindre "Ghost Cache".

### 🛡️ Kritiske endringer:

1.  **Deterministisk Splitting (Ingen AI-gjetting)**:
    *   Systemet spør ikke lenger KI-en om layout (A3 vs A4).
    *   **Ny regel:** Hvis bildet er bredere enn det er høyt (Landskap) etter rotasjon, **SKAL** det splittes i to. Punktum.

2.  **Strict Orientation Pipeline**:
    1.  **Gemini Flash:** Sjekk KUN rotasjon (0, 90, 180, 270). Dette sikrer at ark som er skannet opp-ned blir snudd riktig.
    2.  **Fysisk Rotasjon:** Canvas snur bildet basert på KI-svaret.
    3.  **Geometrisk Sjekk:** Er bildet nå Landskap? -> Splitt.

3.  **Ghost Cache Fix**:
    *   Ved splitting genereres nå `contentHash` basert på den *nye pikseldataen* i de splittede delene.
    *   Dette tvinger systemet til å behandle Venstre og Høyre side som unike, nye filer, og hindrer at innhold fra forrige side (eller originalfilen) lekker inn.

### Teknisk:
*   Forenklet `detectPageLayout` i `geminiService.ts`.
*   Skrevet om loopen i `useProjectProcessor.ts` til en lineær pipeline.
*   Oppdatert `TECHNICAL_STANDARD.md` med Regel 1 og 37.
