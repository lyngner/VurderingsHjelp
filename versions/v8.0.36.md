
# Oppdatering v8.0.36 - Math Syntax Fix (Curly Braces)

Denne oppdateringen løser feilmeldingen "Math input error" som oppstår når KI-en genererer slapp LaTeX-kode for eksponenter og derivasjon.

### 🛡️ Kritiske endringer:

1.  **Curly Brace Mandate**:
    *   Flash-modellen har fått en streng, diktatorisk instruks om å **ALLTID** bruke krøllparenteser `{}` for eksponenter.
    *   **Feil:** `e^2x` (Vises som $e^2 x$ eller gir feil i komplekse uttrykk).
    *   **Korrekt:** `e^{2x}` (Vises korrekt som $e^{2x}$).

2.  **Derivative Safety**:
    *   Spesifikk instruks for å unngå "Double Exponent"-feilen ved derivasjon.
    *   **Feil:** `e^{2x}'` (MathJax tolker dette som at e har to eksponenter: `2x` og `'`).
    *   **Korrekt:** `(e^{2x})'` eller `{e^{2x}}'`.

### Teknisk:
*   Oppdatert `LATEX_MANDATE` i `geminiService.ts`.
*   Lagt til Regel 45 i `TECHNICAL_STANDARD.md`.
*   Systemversjon: **v8.0.36**.
