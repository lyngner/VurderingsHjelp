
# Oppdatering v7.9.15 - Auto-Resume on Network Recovery

Denne oppdateringen løser problemet med at filbehandling stopper opp når datamaskinen går i dvale eller lukkes ("Lid close scenario").

### 🛡️ Kritiske endringer:

1.  **Auto-Resume på Online-event**:
    *   Applikasjonen lytter nå etter nettleserens `online`-signal (som fyres av når du åpner PC-en igjen eller kobler til wifi).
    *   Når signalet mottas, tvinges "låsen" på prosesseringskøen opp, og systemet prøver automatisk å fortsette der det slapp.

2.  **Feilmelding-håndtering**:
    *   Logikken håndterer nå spesifikke `TypeError: Failed to fetch` feilmeldinger som ofte oppstår i Service Worker-miljøer ved nettverksbrudd, og behandler dem som midlertidige nettverksfeil (uendelig retry) i stedet for å feile filen permanent.

### Teknisk:
*   Oppdatert `useProjectProcessor.ts` med `window.addEventListener('online')` og `retryTrigger` state.
*   Systemversjon: **v7.9.15**.
