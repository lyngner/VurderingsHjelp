
# Oppdatering v8.0.43 - Editor Readability & Stray Character Cleanup

Denne oppdateringen løser visuelle feil i LaTeX-visningen og forbedrer brukervennligheten ved redigering av matematisk tekst.

### 🛡️ Kritiske endringer:

1.  **Cleaner Logic (\f removal)**:
    *   **Problemet:** KI-en kunne noen ganger hallusinere isolerte `\f` tegn før brøker (f.eks. `\f \frac{...}`), noe som ga røde "Math input error"-bokser fordi `\f` ikke er en gyldig LaTeX-kommando alene.
    *   **Løsningen:** Oppdatert `LatexRenderer` til å fjerne alle isolerte `\f` som ikke er starten på en gyldig kommando (som `\frac`). Dette reparerer visningen umiddelbart.

2.  **Editor Newlines**:
    *   **Problemet:** Når man redigerte LaTeX-kode, ble linjeskift-kommandoen `\\` vist in-line, noe som gjorde lange utregninger uleselige i editoren.
    *   **Løsningen:** I redigeringsmodus tvinges det nå frem et faktisk linjeskift etter hver `\\`, slik at strukturen blir tydelig for brukeren.

### Teknisk:
*   Oppdatert `LatexRenderer` med regex `clean.replace(/\\f(?![a-zA-Z])/g, '')`.
*   Oppdatert `ReviewStep` editor med `.replace(/\\\\/g, '\\\\\n')`.
*   Systemversjon: **v8.0.43**.
