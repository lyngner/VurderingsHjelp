
# Oppdatering v7.9.3 - Digital Formatting Fix

Denne oppdateringen løser problemet med at strukturen i Word-dokumenter (lister, kodeblokker) ble flatet ut til en uleselig tekstblokk under import.

### 🛡️ Endringer:

1.  **Strukturell Word-import**:
    *   Oppdatert `mammoth`-konfigurasjonen i `fileService.ts`.
    *   Systemet oversetter nå HTML-tags som `<li>` (lister), `<tr>` (tabellrader) og `<pre>` (kode) til eksplisitte linjeskift (`\n`).
    *   Tidligere ble alt dette slått sammen. Nå bevares strukturen slik at Python-kode og punktlister er lesbare for KI-en.

2.  **Tabell-separasjon**:
    *   Lagt inn logikk som setter inn tabulator/mellomrom mellom tabellceller (`<td>`) for å hindre at tall og tekst i tabeller krasjer sammen (f.eks. "Oppgave 1" og "Poeng 2" ble "Oppgave 1Poeng 2").

### Teknisk:
*   Oppdatert `processFileToImages` i `fileService.ts` med utvidet regex-logikk for HTML-rensing.
*   Systemversjon: **v7.9.3**.
