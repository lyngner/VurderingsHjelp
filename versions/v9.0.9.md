
# Oppdatering v9.0.9 - Kompakt Word & Matte-CAS

Denne oppdateringen justerer behandlingen av Word-dokumenter for å gjøre teksten mer kompakt og sikre korrekt matematisk visning.

### 🛡️ Kritiske endringer:

1.  **Mindre luft i tekst (Kompakt Word)**:
    *   Endret logikken for importering av Word-dokumenter. 
    *   Systemet setter nå inn **enkle linjeskift** (`\n`) i stedet for doble (`\n\n`) mellom avsnitt.
    *   Dette gjør teksten mindre "luftig" og mer naturlig å lese, spesielt for dokumenter med mange korte linjer.

2.  **Matematikk i CAS-boks**:
    *   Instruert KI-systemet til å **alltid** bruke LaTeX-omslag (`\( ... \)`) rundt matematiske formler, også når de transkriberes fra CAS-bilder eller Word-skjermbilder.
    *   Dette sørger for at brøker og formler i den grå "Tolkning av Gemini"-boksen blir rendret som matematikk, i stedet for å vises som kildekode.

### Teknisk:
*   Oppdatert `fileService.ts` med `\n` erstatning.
*   Oppdatert `transcribeImageWorker` i `geminiService.ts` med streng LaTeX-instruks.
*   Systemversjon: **v9.0.9**.
