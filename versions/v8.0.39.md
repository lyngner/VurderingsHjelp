
# Oppdatering v8.0.39 - LaTeX Shorthand Repair

Denne oppdateringen løser problemet med at `\f` dukker opp som et rødt feil-symbol i matematikk, ofte som erstatning for `\frac`.

### 🛡️ Kritiske endringer:

1.  **Frontend Shorthand Fix**:
    *   **Problemet:** KI-en (eller JSON-parsing) produserer noen ganger `\f 1 2` i stedet for `\frac{1}{2}` eller `\frac 1 2`. Dette fører til at MathJax viser en rød `\f` feil.
    *   **Løsningen:** `LatexRenderer` søker nå etter `\f` etterfulgt av et ikke-bokstavtegn (mellomrom, krøllparentes, tall). Hvis funnet, erstattes `\f` automatisk med `\frac`.
    *   Dette reparerer visningen umiddelbart, også for eksisterende data.

2.  **No-Shorthand Mandate**:
    *   Oppdatert `LATEX_MANDATE` i systeminstruksene med et eksplisitt forbud mot å bruke `\f` som forkortelse.

### Teknisk:
*   Oppdatert `SharedUI.tsx` med regex-fiks: `clean.replace(/\\f([^a-zA-Z])/g, '\\frac$1')`.
*   Oppdatert `geminiService.ts`.
*   Systemversjon: **v8.0.39**.
