
# Oppdatering v8.2.2 - Universal LaTeX Parity

Denne oppdateringen sikrer at Flash-modellen følger nøyaktig samme strenge LaTeX-regler som Pro-modellen, og reparerer visuelle feil i eksisterende materiale.

### 🛡️ Kritiske endringer:

1.  **Instruction Parity**:
    *   `LATEX_MANDATE` (den strenge regelen om `\begin{aligned}` og `\[ ... \]`) var utelatt fra den nye `generateCriterionForTask`-funksjonen i v8.2.0.
    *   Dette er nå lagt inn, slik at alle modeller tvinges til å bruke korrekt syntaks.

2.  **Frontend Auto-Repair**:
    *   Lagt inn en "vaskemaskin" i `LatexRenderer` som automatisk fikser vanlige feil som mellomrom i delimitere (`\ [` -> `\[`) og manglende wrappers rundt `aligned`-blokker.
    *   Dette gjør at selv "ødelagte" formler fra forrige versjon nå vil vises korrekt uten at du trenger å gjøre noe.

### Teknisk:
*   Oppdatert `generateCriterionForTask` i `geminiService.ts`.
*   Oppdatert regex i `SharedUI.tsx`.
*   Systemversjon: **v8.2.2**.
