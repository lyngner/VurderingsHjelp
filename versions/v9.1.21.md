
# Oppdatering v9.1.21 - The Big Scrub (LaTeX Cleaning)

Denne oppdateringen innfører en aggressiv "vaskemaskin" for LaTeX-kode for å fikse gjentagende visningsfeil som "Missing \end" og `\b`-støy.

### 🛡️ Kritiske Feilrettinger:

1.  **`\b` Artifact Removal**:
    *   **Problemet:** KI-en la ofte inn `\b` (sannsynligvis ment som bullet point eller bold) rett før `\begin{aligned}`. Siden `\b` i LaTeX er en kommando som krever et argument, "spiste" den opp `\begin` og ødela hele blokken.
    *   **Løsningen:** `LatexRenderer` skanner nå teksten for `\b` etterfulgt av `\begin` eller `\text`, og sletter `\b` kirurgisk. Den fjerner også `\b` som står alene.

2.  **Cases Balancing**:
    *   **Problemet:** Mange oppgaver (spesielt delt forskrift) brukte `\begin{cases}` men glemte å lukke med `\end{cases}`.
    *   **Løsningen:** Systemet teller nå start- og slutt-tagger og legger til manglende `\end{cases}` automatisk på slutten av teksten.

3.  **Tag Recovery**:
    *   Fikser situasjoner der `\begin` har blitt til `egin` eller `\end` til `nd` pga JSON-parsing.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx`.
*   Systemversjon: **v9.1.21**.
