
# Oppdatering v9.1.2 - CAS Balancing & Layout Trim

Denne oppdateringen løser "Extra \left or missing \right" feil i CAS-utregninger og fjerner unødvendig luft rundt figurene.

### 🛡️ Kritiske endringer:

1.  **MathJax Balancing Fix**:
    *   **Problemet:** CAS-blokker (linje-for-linje utregning) krasjet MathJax når de inneholdt komplekse parenteser som `\left\{` og `\right\}`, fordi linjeskiftene i blokken kunne splitte par som hører sammen.
    *   **Løsningen:** Systemet stripper nå automatisk kommandoene `\left` og `\right` fra CAS-teksten før visning. De visuelle krøllparentesene `{ }` består, men kravet om streng balansering på hver linje forsvinner.
    *   Dette gjør visningen robust selv om KI-en deler opp utregninger over flere linjer.

2.  **Layout Tightening**:
    *   **Whitespace-trim:** Tekstblokker som ligger rett over eller under en figur-boks blir nå automatisk strippet for tomme linjer (`newlines`) i grensesnittet mot boksen.
    *   **Mindre padding:** Marginene rundt CAS-boksen er redusert ytterligere (`my-1`, `p-2`) for en mer kompakt og integrert følelse.

### Teknisk:
*   Oppdatert `SharedUI.tsx` med regex-fjerning av `\left`/`\right` og smart `trim()` logikk.
*   Systemversjon: **v9.1.2**.
