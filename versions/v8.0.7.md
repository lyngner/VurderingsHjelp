
# Oppdatering v8.0.7 - LaTeX Sanitizer & Math Input Error Fix

Denne oppdateringen løser kritiske problemer med "Math input error" (gule bokser) som oppstod ved derivasjon og andre komplekse matematiske uttrykk.

### 🛡️ Kritiske endringer:

1.  **LaTeX Sanitization (Double Exponent Fix)**:
    *   **Problemet:** Gemini-modellen genererte ofte ugyldig LaTeX-syntaks som `e^{2x}'` ved derivasjon. MathJax tolket dette som at `e` hadde to eksponenter (`2x` og `'`), noe som førte til krasj.
    *   **Løsningen:** Implementert en "Sanitizer" i `SharedUI.tsx` som scanner LaTeX-koden før visning. Den finner automatisk mønstre som `^{...}'` og pakker dem inn i krøllparenteser: `{e^{2x}}'`.
    *   Dette fikser feilen umiddelbart uten at brukeren trenger å regenerere svaret.

2.  **Robust Rendering**:
    *   Sanitizeren retter også opp i literale linjeskift (`\n`) for å sikre korrekt formatering.

### Teknisk:
*   Oppdatert `LatexRenderer` i `components/SharedUI.tsx`.
*   Systemversjon: **v8.0.7**.
