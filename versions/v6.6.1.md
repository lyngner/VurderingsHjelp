
# Oppdatering v6.6.1 - Regex Rescue & Queue Safety

Denne oppdateringen løser problemene med manglende oppgave-badger og treghet i prosesseringskøen.

### 🛡️ Kritiske endringer:

1.  **Regex Rescue (Lokal Badge-redning)**:
    *   Implementert en lokal sjekk som skanner transkripsjonen etter oppgaver (f.eks. "1 a)", "1a") som AI-en har oversett i metadataene.
    *   Hvis en slik oppgave finnes i teksten, OG den er i rettemanualen (whitelist), blir den nå lagt til automatisk. Dette sikrer at læreren ser badgen selv om AI-en var unøyaktig i strukturen.

2.  **Queue Safety Lock**:
    *   Prosessoren har fått en strengere sperre som hindrer den i å starte behandling av elevfiler før rettemanualen faktisk inneholder kriterier.
    *   Dette hindrer "tomgangskjøring" der systemet raser gjennom filer uten å kunne tilegne dem oppgaver fordi filteret var tomt.

### Teknisk:
*   Oppdatert `integratePageResults` i `hooks/useProjectProcessor.ts` med regex-logikk.
*   Lagt til sjekk på `rubric.criteria.length` før oppstart av `processQueue`.
