
# Oppdatering v8.9.41 - Queue Management

Denne oppdateringen gir deg mer kontroll over innlastingskøen ved å tillate sletting av ventende filer.

### 🛡️ Endringer:

1.  **Fjern fra kø (Sletteknapp)**:
    *   Lagt til en **`✕`** knapp på alle filer i "Glass Box"-listen (den som viser hva som analyseres).
    *   Dette lar deg avbryte og fjerne filer som er lagt til ved en feil, eller som har feilet/henger, uten å måtte laste siden på nytt.
    *   Hvis du sletter filen som analyseres *akkurat nå* (blå status), vil systemet automatisk avbryte API-kallet mot Google før filen fjernes.

2.  **Opprydding**:
    *   Filen slettes også fysisk fra nettleserens bildelager (`deleteMedia`) for å spare plass.

### Teknisk:
*   Utvidet `useProjectProcessor.ts` med `handleDeleteUnprocessedPage`.
*   Oppdatert `SetupStep.tsx` med knapp for fjerning.
*   Systemversjon: **v8.9.41**.
