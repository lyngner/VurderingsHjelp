
# Oppdatering v7.9.2 - Cross-Type Sibling Inference

Denne oppdateringen løser feilen der deler av en besvarelse "forsvant" hvis eleven leverte en kombinasjon av bilder (.jpg) og Word-dokumenter (.docx).

### 🛡️ Kritisk Feilretting:

1.  **Filtype-agnostisk Matching**:
    *   **Før:** Systemet krevde at "søsken-sider" (sider fra samme filnavn-stamme) måtte ha identisk filnavn inkludert extension for å kobles. Dette gjorde at `Candidate 101.jpg` og `Candidate 101.docx` ikke fant hverandre.
    *   **Nå:** Systemet stripper vekk filendelser (.jpg, .pdf, .docx) og suffikser ((V), (H)) før sammenligning.
    *   **Resultat:** Hvis bildene (Del 1) mangler ID, men Word-filen (Del 2) har ID, vil Word-filen nå "redde" bildene og samle alt under riktig kandidat.

2.  **Tryggere Sammenslåing**:
    *   Endret logikken for sletting av den gamle "Ukjent"-kandidaten fra `splice` (indeks-basert) til `filter` (ID-basert). Dette hindrer potensielle race-conditions der feil kandidat kunne bli slettet ved rask prosessering.

### Teknisk:
*   Oppdatert `integratePageResults` i `hooks/useProjectProcessor.ts` med `cleanBaseName` hjelpefunksjon.
*   Forbedret "Rescue"-logikken til å bruke `filter` for atomisk sletting.
