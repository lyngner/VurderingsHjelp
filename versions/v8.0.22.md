
# Oppdatering v8.0.22 - Greedy Anti-Stutter (Fix)

Denne oppdateringen innfører en aggressiv regex-vask for å fjerne "stotring" i oppgavenummer fra både OCR og Rubric-generering.

### 🛡️ Kritiske endringer:

1.  **Diktatorisk "Fix-Stutter"**:
    *   Innført en hjelpefunksjon `fixStutter(str)` som kjøres på ALLE oppgavenummer før de behandles videre.
    *   **Logikk:**
        *   `11` -> `1`
        *   `22` -> `2`
        *   `11a` -> `1a`
        *   `22b` -> `2b`
    *   Dette kjøres uavhengig av om oppgaven finnes i rettemanualen eller ikke, som et preventivt tiltak mot modellens tendens til å repetere tokens.

2.  **Rubric & OCR**:
    *   Fixen brukes nå både når rettemanualen *lages* (slik at fasiten ikke inneholder "Oppgave 11") og når elevsvar *leses* (slik at svarene mappes riktig).

### Teknisk:
*   Implementert `fixStutter` i `geminiService.ts`.
*   Systemversjon: **v8.0.22**.
