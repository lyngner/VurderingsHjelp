
# Oppdatering v8.9.22 - LaTeX Misplaced Alignment Fix

Denne oppdateringen løser en utbredt visningsfeil ("Misplaced &") i matematikk-blokker.

### 🛡️ Løsning: "Global Alignment Wrapper"

1.  **Problemet**:
    *   Når KI-en genererer en blokk med `\begin{aligned}... \end{aligned}`, hender det at den glemmer å pakke denne inn i matematiske delimitere (`\[ ... \]`).
    *   Hvis denne blokken i tillegg blandes med vanlig tekst, forstår ikke MathJax at dette skal være et "display math"-miljø, og krasjer på `&`-tegnet.

2.  **Løsningen**:
    *   `LatexRenderer` kjører nå en **global regex-søk** etter alle `aligned`-miljøer.
    *   Hvis et miljø ikke allerede er innpakket i `\[` eller `\(`, setter systemet automatisk inn `\[ ... \]` rundt det.
    *   Samtidig renses blokken for tomme linjer (`\n\n` -> `\n`), som også er en kjent kilde til feil i `aligned`-miljøer.

3.  **Resultat**:
    *   Løsningsforslag og elevsvar som tidligere viste gul feilmelding, skal nå vises korrekt formatert automatisk.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx`.
*   Systemversjon: **v8.9.22**.
