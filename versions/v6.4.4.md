
# Oppdatering v6.4.4 - Queue Stability & Error Handling

Denne oppdateringen løser kritiske problemer med "hengende" filer i innlastingskøen og 400 Bad Request-feil på Word-dokumenter.

### 🛡️ Endringer i v6.4.4:

1.  **Infinite Retry Fix (Local Blacklist)**:
    *   Innført en `failedIds` mekanisme i prosesseringsloopen. 
    *   Hvis en fil feiler (f.eks. pga. 400 Bad Request eller nettverksfeil), legges den nå umiddelbart i en lokal svarteliste for den gjeldende sesjonen.
    *   Dette hindrer loopen i å plukke opp samme fil om og om igjen hvis React-statet er tregt med å oppdatere, og lar systemet gå videre til neste fil (f.eks. Word-dokumentene dine) i stedet for å stoppe opp.

2.  **Word Document 400 Fix**:
    *   Lagt inn en sjekk i `analyzeTextContent` som stopper tomme eller ugyldige tekstpayloads fra å bli sendt til Gemini API. Dette er ofte årsaken til `400 Bad Request` på digitale filer.

3.  **Versjonsstyring**:
    *   Oppdatert `SYSTEM_VERSION` til v6.4.4.
