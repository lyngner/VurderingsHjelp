
# Oppdatering v8.9.15 - Math Stability & Print Vis

Denne oppdateringen fikser kritiske feil med matematisk rendring i "Ting å jobbe med", samt en spesifikk feilmelding ("Misplaced &") ved visning av `aligned` miljøer.

### 🛡️ Kritiske feilrettinger:

1.  **"Misplaced &" Fix**:
    *   **Problemet:** MathJax krasjer med "Misplaced &" hvis den møter en `\begin{aligned}` blokk som ikke er pakket inn i matematiske delimitere (`\[ ... \]`). Dette skjer noen ganger når KI-en genererer komplekse utregninger.
    *   **Løsningen:** `LatexRenderer` oppdager nå automatisk slike "nakne" blokker og pakker dem inn før visning. Dette fikser feilen umiddelbart.

2.  **Matte i Vekstpunkter**:
    *   Tekst i "Ting å jobbe med" kjøres nå gjennom `LatexRenderer`. Dette gjør at formler som `\lg(2x)` vises korrekt som matematikk i stedet for kildekode.

3.  **Snitt i Utskrift**:
    *   Den stiplede linjen for "Snitt" i ferdighetsprofilen er nå synlig på PDF-utskrift, med korrekt tegnforklaring.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx`.
*   Oppdatert `CandidateReport` og `SkillRadarChart` i `ResultsStep.tsx`.
*   Systemversjon: **v8.9.15**.
