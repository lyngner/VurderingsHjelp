
# Oppdatering v7.8.6 - Smart Prefix Scan

Denne oppdateringen gjør OCR-motoren (Flash) smartere til å kjenne igjen oppgaver som er skrevet med ulik syntaks, og forbedrer analysen av digitale dokumenter.

### 🛡️ Endringer:

1.  **Prefix-toleranse**:
    *   Systemet ignorerer nå "støyord" foran oppgavenummer.
    *   Eksempel: Hvis eleven skriver **"Opg. 1a"** eller **"Oppgave 2"**, skjønner KI-en nå at dette er **"1a"** og **"2"**. Tidligere kunne dette føre til at oppgaven ble oversett eller feilkategorisert.

2.  **Heltallsoppgaver**:
    *   Bedre støtte for oppgaver som ikke har bokstaver (f.eks. "Oppgave 3"). Systemet returnerer nå dette som `taskNumber: "3", subTask: ""` i stedet for å finne på en bokstav.

3.  **Multimodal Word-analyse**:
    *   Word-dokumenter analyseres nå med støtte for innebygde bilder. Hvis en elev har limt inn et bilde av en graf i Word, vil KI-en nå "se" dette bildet og beskrive det i `[AI-TOLKNING]`, i stedet for å bare lese teksten rundt.

### Teknisk:
*   Oppdatert `geminiService.ts` med `SMART PREFIX SCAN` instruks.
*   Utvidet `analyzeTextContent` til å ta imot `attachedImages`.
