
# Oppdatering v8.0.37 - Robust LaTeX Sanitizer

Denne oppdateringen er en "Frontend-vask" som rydder opp i vanlige LaTeX-feil produsert av KI-en, slik at læreren slipper gule "Math input error"-bokser.

### 🛡️ Kritiske endringer:

1.  **Frontend Double Exponent Guard**:
    *   **Problemet:** KI-en skriver noen ganger `e^x'` (derivert av e opphøyd i x) uten parenteser. MathJax tolker dette som at `e` har to eksponenter (`x` og `'`), og krasjer.
    *   **Løsningen:** `LatexRenderer` (visningsmotoren) skanner nå teksten automatisk for slike mønstre og setter inn krøllparenteser `{e^x}'` *før* MathJax får se koden.
    *   Dette fikser feilen umiddelbart i visningen, uten at du trenger å skanne siden på nytt.

2.  **Dollar-Sanering**:
    *   Fjernet `$` som lovlig tegn for matematikk i oppsettet.
    *   **Hvorfor?** Hvis en oppgavetekst inneholder dollartegn (f.eks. om valuta) eller KI-en bruker Markdown-fet skrift (`**`), kunne det tidligere trigge MathJax feilaktig. Nå kreves det eksplisitte `\( ... \)` tagger for at noe skal bli til matte.

### Teknisk:
*   Oppdatert Regex i `SharedUI.tsx` for å fange opp både `e^{...}'` og `e^...'`.
*   Fjernet `['$', '$']` fra `MathJax.tex.inlineMath` i `index.html`.
*   Systemversjon: **v8.0.37**.
