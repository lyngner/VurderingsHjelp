
# Oppdatering v8.0.34 - Visual Evidence Hydration Fix

Denne oppdateringen løser en kritisk regresjon introdusert i v8.0.30 der visuelle bevis (CAS/Graf) ble vist som "Se visualEvidence" i stedet for faktisk innhold.

### 🛡️ Kritiske endringer:

1.  **Frontend Hydration Logic**:
    *   Implementert en automatisk erstatter i `ReviewStep.tsx` som finner platsholderen `[BILDEVEDLEGG: Se visualEvidence]` og bytter den ut med innholdet fra databasefeltet `visualEvidence`.
    *   Dette reparerer visningen umiddelbart for alle sider som ble skannet med v8.0.30-33, uten at brukeren trenger å skanne på nytt.

2.  **Reversering av Prompt**:
    *   Endret instruksene i `geminiService.ts` tilbake til en "Embedded" strategi.
    *   KI-en instrueres nå til å lime inn *hele beskrivelsen* direkte i teksten, i tillegg til å legge den i det separate feltet. Dette er mer robust og mindre avhengig av frontend-logikk.

### Hvorfor?
*   Versjon 8.0.30 prøvde å være smart ved å bruke en platsholder for å spare tokens i hovedteksten, men manglet koden for å flette innholdet tilbake ved visning. v8.0.34 tetter dette hullet.

### Teknisk:
*   Oppdatert `ReviewStep.tsx` med `hydrateEvidence` funksjon.
*   Oppdatert `VISUAL_MANDATE` i `geminiService.ts`.
*   Systemversjon: **v8.0.34**.
