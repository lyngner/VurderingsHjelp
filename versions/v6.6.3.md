
# Oppdatering v6.6.3 - Multi-Evidence Interleaving

Denne oppdateringen svarer på behovet for å kunne ha flere grå "tolkefelt" (Visual Evidence) spredt utover i besvarelsen, spesielt for digitale dokumenter der tekst, figurer og kode ofte kommer om hverandre.

### 🛡️ Kritiske endringer:

1.  **Multiple Interleaved Boxes**:
    *   Fjernet begrensningen som instruerte KI-en om å "flytte" alt teknisk innhold til ett samlet felt på slutten.
    *   **Ny Instruks:** KI-en er nå beordret til å integrere tolkningen av figurer/kode **direkte i teksten** (`fullText`) ved å bruke taggen `[AI-TOLKNING AV FIGUR: ...]`.
    *   Dette betyr at hvis en elev har tre grafer på ulike steder i teksten, vil systemet nå generere tre separate grå bokser på nøyaktig de stedene grafene befinner seg.

2.  **Digital Flyt (Word/PDF)**:
    *   Spesielt optimalisert for `analyzeTextContent` (Word-analyse), der Python-kode og CAS-utregninger nå vil vises in-line i stedet for som et vedlegg.

### Teknisk:
*   Oppdatert `systemInstruction` i `geminiService.ts` for både `analyzeTextContent` og `transcribeAndAnalyzeImage`.
*   Prompten oppfordrer nå til "Interleaved Evidence" i stedet for "Field Separation".
