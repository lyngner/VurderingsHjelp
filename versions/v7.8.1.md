
# Oppdatering v7.8.1 - Network Resilience

Denne oppdateringen løser problemet med at innlasting av elevbesvarelser stopper opp hvis internettforbindelsen er ustabil.

### 🛡️ Kritiske endringer:

1.  **Evighets-retry (Regel 39)**:
    *   Tidligere ga systemet opp og markerte en side som "Feil" hvis et API-kall feilet (f.eks. nettbrudd). Dette ødela hele batchen.
    *   **Nå:** Hvis systemet oppdager en nettverksfeil (`fetch failed`, `NetworkError`, `503`, `504`), pauser det i 5 sekunder.
    *   Deretter prøver det **samme side på nytt**.
    *   Dette fortsetter i en uendelig løkke helt til internett kommer tilbake. Ingen filer blir markert som feil, og prosessen fortsetter automatisk der den slapp.

2.  **Status-indikasjon**:
    *   Statuslinjen i appen vil vise "**Venter på internett... (Prøver igjen om 5s)**" mens den venter, slik at læreren forstår hvorfor ting tar tid.

### Teknisk:
*   Oppdatert `processQueue` i `useProjectProcessor.ts` med en dedikert `catch`-blokk for nettverksfeil som trigger `continue` i stedet for feilhåndtering.
*   Lagt til Regel 39 i `TECHNICAL_STANDARD.md`.
