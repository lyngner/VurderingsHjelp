# Oppdatering v5.4.3 - Vision & Stability Anchor

Denne oppdateringen løser kritiske feil ved sletting og gjenoppretter avansert figurtolking.

### 🛡️ Kritiske endringer i v5.4.3:

1.  **Stability Lock (Slette-fix)**:
    *   Løst bug der sletting av en side under pågående innlasting kunne føre til at alle kandidater forsvant fra oversikten.
    *   Sletting er nå en isolert operasjon som ikke påvirker andre asynkrone prosesser.

2.  **Deep Visual Evidence (CAS & Grafer)**:
    *   Forsterket instruks til Gemini: Alt visuelt innhold (CAS-bilder, grafer, skjermutklipp) SKAL beskrives.
    *   Beskrivelsen legges i den mørkegrå boksen merket "AI-tolkning av figur".

3.  **Orientation Guard v2**:
    *   Lagt til en dedikert sjekk for 180-graders rotasjon (opp-ned).
    *   Systemet roterer bildet fysisk før transkribering dersom KI-en detekterer at arket ligger feil vei.

4.  **Versjonsstyring**:
    *   Oppdatert `SYSTEM_VERSION` til v5.4.3.
    *   Bekreftet at alle regler fra v5.3.7 (matematisk stabilitet) er aktive.

### Teknisk:
*   Refaktorert `handleDeletePage` i `App.tsx` med redundante sjekker.
*   Oppdatert `LatexRenderer` i `SharedUI.tsx` for å fange opp flere varianter av figur-tagger.
*   Oppdatert `geminiService.ts` med strengere visuelle krav.