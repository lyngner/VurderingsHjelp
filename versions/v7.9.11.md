
# Oppdatering v7.9.11 - Hash Collision Fix

Denne oppdateringen løser en kritisk feil der sider med lite håndskrift kunne bli forvekslet med hverandre i systemets hurtigbuffer (cache).

### 🛡️ Kritisk Feilretting:

1.  **Robust Hashing (Fix av "Feil Side")**:
    *   **Problemet:** Den tidligere metoden for å generere unike IDer for bilder (hashing) brukte bare 3 små utsnitt av bildefilen (start, midt, slutt).
    *   **Konsekvensen:** Hvis to elever leverte sider som var nesten helt like (f.eks. to nesten tomme ark med ruter), kunne systemet tro det var samme bilde. Resultatet var at transkripsjonen fra Elev A dukket opp på Elev B sin side ("Ghosting").
    *   **Løsningen:** Hash-algoritmen er nå skrevet om fullstendig. Den inkluderer nå filstørrelsen i ID-en, og tar prøver fra hele filen (hver 1000. tegn) i stedet for bare 3 steder. Dette garanterer at selv små forskjeller i håndskrift gir en unik ID.

2.  **Automatisk Cache-busting**:
    *   Siden hashing-algoritmen er endret, vil alle *nye* opplastinger automatisk ignorere den gamle cachen. Dette "helbreder" problemet umiddelbart for nye prosjekter.

### Teknisk:
*   Totalrenovert `generateHash` i `fileService.ts`.
*   Systemversjon: **v7.9.11**.
