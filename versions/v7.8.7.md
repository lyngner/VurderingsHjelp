
# Oppdatering v7.8.7 - Service-Level Whitelist

Denne oppdateringen er en kritisk feilretting for å stoppe KI-en fra å finne på ("hallusinere") oppgaver som ikke eksisterer i fasiten.

### 🛡️ Endringer:

1.  **Service-Level Filtering**:
    *   Innført et nådeløst filter direkte i API-laget (`geminiService.ts`).
    *   Etter at Flash-modellen har analysert en side, går resultatet gjennom en "vaskemaskin".
    *   **Regel:** Enhver oppgave som Flash har funnet (f.eks. "1c") blir sjekket mot den aktive rettemanualen. Hvis "1c" ikke finnes i manualen, blir den **slettet umiddelbart** fra resultatlisten.
    *   Dette garanterer at ingen ugyldige "badger" (oppgavemerker) noensinne når brukergrensesnittet.

2.  **Hvorfor?**:
    *   Etter v7.8.6 ble Flash-modellen så ivrig på å finne oppgaver at den begynte å tolke sidetall og annen tekst som nye oppgaver. Dette filteret stopper den oppførselen hardt.

### Teknisk:
*   Lagt til `filterTasksAgainstRubric` i `geminiService.ts`.
*   Oppdatert `transcribeAndAnalyzeImage` og `analyzeTextContent` til å bruke dette filteret før retur.
