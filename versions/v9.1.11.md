
# Oppdatering v9.1.11 - Hard Error Resilience

Denne oppdateringen løser kritiske problemer der innlastingskøen stoppet opp på grunn av uventede API-feil, og gjør systemet bedre til å rapportere hva som faktisk gikk galt.

### 🛡️ Kritiske endringer:

1.  **Error Exposure (400/404)**:
    *   Systemet rapporterer nå nøyaktige feilmeldinger ("Modell ikke funnet", "Ugyldig forespørsel") i stedet for å falle inn i en "Rotasjons-loop".
    *   Filer som feiler på grunn av harde API-feil (ikke nettverk/kvote) blir nå markert som **Feilet** umiddelbart, slik at køen kan fortsette til neste fil.

2.  **Rotation Logic Hardening**:
    *   Systemet forsøker nå KUN å rotere filer hvis de faktisk er bilder (`image/*`). Dette hindrer krasj når Word-dokumenter feiler i analysen.
    *   Rotasjon trigges ikke lenger på harde API-feil (400-serien), da rotasjon ikke hjelper på en ugyldig forespørsel.

3.  **Feilsøking**:
    *   Fullstendige feilmeldinger logges nå til nettleser-konsollen (F12) for å gjøre det enklere å forstå hvorfor API-et avviser forespørselen (f.eks. feil modellnavn).

### Teknisk:
*   Oppdatert `useProjectProcessor.ts` med strengere feilhåndtering.
*   Lagt til `console.error` i `geminiService.ts`.
*   Systemversjon: **v9.1.11**.
