
# Oppdatering v8.0.52 - Resultat-vask (Double Letter Bug)

Denne oppdateringen løser feilen der sensoren returnerte oppgavenavn med doble bokstaver ("1bb" i stedet for "1b"), noe som ødela resultattabellen.

### 🛡️ Kritiske endringer:

1.  **Doble Bokstaver (Anti-Stutter)**:
    *   **Problemet:** Sensoren (Pro-modellen) har en tendens til å være "hjelpsom" ved å repetere informasjon. Hvis den blir bedt om `taskNumber` og `subTask`, svarer den ofte `taskNumber: "1b"` og `subTask: "b"`.
    *   **Konsekvensen:** Frontend-koden slår dette blindt sammen til "1bb". Siden rettemanualen heter "1b", finner ikke systemet en match, og resultatet forsvinner fra grafer og tabeller.
    *   **Løsningen:** Funksjonen `cleanTaskPair` er oppgradert. Den oppdager nå automatisk om `taskNumber` slutter med `subTask` (f.eks. "1b" og "b"). Hvis ja, stripper den suffikset slik at vi sitter igjen med rene "1" og "b".

2.  **Robusthet**:
    *   Lagt til anti-stotring for underoppgaver ("bb" -> "b") i samme slengen.
    *   Lagt til ekstra logikk for tilfeller der underoppgaven mangler men er bakt inn i nummeret ("1a" -> "1", "a").

### Teknisk:
*   Oppdatert `cleanTaskPair` i `geminiService.ts` med avansert suffix-matching.
*   Systemversjon: **v8.0.52**.
