
# Oppdatering v8.5.7 - Visuell Word-visning

Denne oppdateringen innfører en visuell forhåndsvisning av Word-dokumenter i Kontroll-steget, slik at læreren kan se originaldokumentet (tabeller, formler, bilder) på samme måte som PDF-er og bilder.

### 🛡️ Endringer:

1.  **Visuell Word-motor (`docx-preview`)**:
    *   Integrert biblioteket `docx-preview`.
    *   I "Kontroll"-steget vises nå Word-dokumenter som **faktiske dokumenter** (hvite ark med layout, tabeller, fonter og bilder) på venstre side, i stedet for en kjedelig tekstboks.
    *   Dette gjør det mye lettere å se hva eleven faktisk mente, spesielt med tabeller og formler.

2.  **Lagring av Originalfil**:
    *   Systemet lagrer nå en binærkopi av `.docx`-filen i nettleserens database ved opplasting.
    *   Dette gjør at vi kan rekonstruere visningen når som helst uten å måtte laste opp filen på nytt.

3.  **Hybrid Visning**:
    *   **Venstre side:** Visuell Word-visning (slik eleven leverte).
    *   **Høyre side:** AI-ens tolkning (LaTeX/Tekst) som brukes til vurdering.

### Teknisk:
*   Oppdatert `index.html` med import.
*   Oppdatert `fileService.ts` for å lagre blob.
*   Ny komponent `DocxRenderer` i `SharedUI.tsx`.
*   Oppdatert `ReviewStep.tsx` for å bruke rendereren.
*   Systemversjon: **v8.5.7**.
