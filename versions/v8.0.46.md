
# Oppdatering v8.0.46 - Lazy Loading Fix (Sticky Load)

Denne oppdateringen fikser bugen der bilder ble stående fast i "Venter på skroll..."-modus.

### 🛡️ Kritiske endringer:

1.  **Sticky Loading**:
    *   Endret logikken i bildevisningen (`LazyImage`) slik at den **aldri sletter bildet fra minnet** når det først er lastet inn.
    *   Tidligere prøvde systemet å spare minne ved å "unload"-e bilder du scrollet forbi. Dette førte til flimring og at bilder av og til hang seg opp.
    *   Nå kobles `IntersectionObserver` fra ("disconnect") så snart bildet er synlig første gang.

2.  **Økt Buffer**:
    *   Økte `rootMargin` til 600px. Dette betyr at bildene begynner å laste inn lenge før du scroller ned til dem, noe som gir en sømløs opplevelse.

### Teknisk:
*   Refaktorert `LazyImage` i `ReviewStep.tsx`.
*   Systemversjon: **v8.0.46**.
