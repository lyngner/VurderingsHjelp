
# Oppdatering v7.9.14 - Derivative Syntax Fix

Denne oppdateringen løser et spesifikt problem med LaTeX-rendering der KI-en genererte ugyldig kode for den deriverte av uttrykk med eksponenter.

### 🛡️ Kritiske feilrettinger:

1.  **"Double Exponent" Guard**:
    *   Fikset feilmeldingen *"Prime causes double exponent: use braces to clarify"* som oppstod ved derivasjon av eksponentielle funksjoner (f.eks. `(e^{2x})'`).
    *   Tidligere kunne KI-en skrive `e^{2x}'`, noe som MathJax tolker som at `e` har to eksponenter (`2x` og `'`). 
    *   KI-en er nå strengt instruert til å bruke parenteser rundt slike uttrykk: `(e^{2x})'`.

2.  **Generell Derivasjonshygienen**:
    *   Lagt inn en bredere regel i `LATEX_MANDATE` som forbyr blanding av apostrof `'` og hatt `^` på samme base uten beskyttende parenteser.

### Teknisk:
*   Oppdatert `LATEX_MANDATE` i `geminiService.ts`.
*   Systemversjon: **v7.9.14**.
