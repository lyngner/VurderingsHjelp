
# Oppdatering v7.9.8 - Timeout Guard & Batch Fix

Denne oppdateringen løser problemet med at applikasjonen kunne "henge" i evig tid ved nettverksproblemer, og fikser visningen av fremdriftslinjen.

### 🛡️ Kritiske endringer:

1.  **API Timeout Guard (60s)**:
    *   Innført en hard tidsbegrensning på alle kall mot Gemini API.
    *   Hvis et kall tar mer enn 60 sekunder, avbrytes det nå lokalt, og systemet prøver automatisk på nytt (Retry).
    *   Dette hindrer at "Spøkelses-kall" låser hele prosesseringskøen hvis Google sin server slutter å svare uten å sende feilmelding.

2.  **Dynamisk Batch-teller**:
    *   Fikset logikken for fremdriftslinjen ("Side X av Y").
    *   Telleren oppdateres nå dynamisk hvis køen vokser underveis (f.eks. når et A3-ark splittes i to).
    *   Dette sikrer at prosenten er nøyaktig og at brukeren ikke ser "105%" ferdig.

### Teknisk:
*   Oppdatert `withRetry` i `geminiService.ts` med `Promise.race`.
*   Oppdatert `processQueue` i `useProjectProcessor.ts` med rekursiv total-beregning.
*   Systemversjon: **v7.9.8**.
