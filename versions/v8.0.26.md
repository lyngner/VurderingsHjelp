
# Oppdatering v8.0.26 - Field Redundancy Fix (The Canonical Anchor)

Denne oppdateringen løser det dypere problemet der oppgaver fikk navn som "11a" fordi koden slo sammen "1" (taskNumber) og "1a" (subTask).

### 🛡️ Kritiske endringer:

1.  **Redundancy Vask (`cleanTaskPair`)**:
    *   Systemet sjekker nå om `subTask` starter med `taskNumber`.
    *   **Eksempel:** Hvis rettemanualen sier task: "1", sub: "1a", vil koden nå automatisk korrigere det til task: "1", sub: "a".
    *   Dette fjerner "11a" fra tabellhodet og sikrer at både fasit og elevbesvarelser bruker nøyaktig samme format.

2.  **Symmetrisk Matching**:
    *   Både ved generering av fasit og ved lesing av elevsvar brukes nå denne par-vasken. 
    *   Dette garanterer at "1a" hos eleven alltid matcher "1a" i fasiten, selv om KI-modellen er overivrig med å gjenta tall.

3.  **UI Konsistens**:
    *   Alle badges og tabellhoder bruker nå en sentral `renderTaskLabel` funksjon som garanterer ren visning.

### Hvorfor feilet Pro-modellen?
*   KI-modeller (også Pro) prøver ofte å være "hjelpsomme" ved å inkludere kontekst (tallet 1) i alle underoppgaver (1a). Dette skaper problemer når koden vår forventer at feltene er helt isolerte. v8.0.26 fjerner dette gapet mellom menneskelig logikk og KI-output.

### Teknisk:
*   Implementert `cleanTaskPair` i `geminiService.ts`.
*   Oppdatert `filterTasksAgainstRubric`, `generateRubricFromTaskAndSamples` og `improveRubricWithStudentData`.
*   Systemversjon: **v8.0.26**.
