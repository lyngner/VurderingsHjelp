
# Oppdatering v8.2.1 - LaTeX Wrapper Fix

Denne oppdateringen løser problemet med at matematikk i rettemanualen ble vist som råtekst (`\begin{aligned}...`) i stedet for renderet matematikk.

### 🛡️ Kritiske feilrettinger:

1.  **Frontend Auto-Wrapper**:
    *   **Problemet:** KI-en produserte noen ganger "nakne" `aligned`-blokker uten de nødvendige `\[ ... \]` delimitere rundt. Dette førte til at MathJax ignorerte innholdet.
    *   **Løsningen:** `LatexRenderer` i frontend oppdager nå automatisk om en `aligned`-blokk mangler innpakning, og setter inn delimitere før visning.
    *   Dette reparerer visningen umiddelbart for både gamle og nye rettemanualer uten behov for regenerering.

2.  **Instruction Mandate**:
    *   Oppdatert systeminstruksen for rettemanual-generering med et eksplisitt krav: `MUST BE WRAPPED IN \[ ... \]`.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx` med regex-wrapper.
*   Oppdatert `generateCriterionForTask` i `geminiService.ts`.
*   Systemversjon: **v8.2.1**.
