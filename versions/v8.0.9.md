
# Oppdatering v8.0.9 - Immediate Retry UI Fix

Denne oppdateringen løser en visuell bug der filer som ble "Hoppet over" ikke oppdaterte statusen sin i listen umiddelbart etter at brukeren trykket "Kjør" (Retry).

### 🛡️ Kritiske endringer:

1.  **Atomic UI Update (Retry)**:
    *   Endret logikken i `handleRetryPage` til å bruke en direkte, funksjonell tilstandsoppdatering (`setActiveProject(prev => ...)`).
    *   Dette garanterer at statusen endres fra `skipped` til `pending` (og at status-teksten endres til "I kø") *umiddelbart* ved klikk, uavhengig av asynkrone prosesser i bakgrunnen.
    *   Fjerner forvirring der brukeren trykket "Kjør", men filen ble stående som "Hoppet over" visuelt.

2.  **Dokumentasjon**:
    *   Lagt til manglende logger for v8.0.7 og v8.0.8.

### Teknisk:
*   Refaktorert `handleRetryPage` i `hooks/useProjectProcessor.ts`.
*   Systemversjon: **v8.0.9**.
