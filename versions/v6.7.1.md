
# Oppdatering v6.7.1 - Mandatory Universal Split

Denne oppdateringen innfører en absolutt regel om at alle bilde-filer skal splittes i to ved innlasting, uavhengig av innhold.

### 🛡️ Kritiske endringer:

1.  **Tvungen Splitting**:
    *   **Landskap (Bredde > Høyde):** Bildet klippes vertikalt (Venstre/Høyre). 
    *   **Portrett (Høyde > Bredde):** Bildet klippes horisontalt (Topp/Bunn).
    *   Ingen AI er involvert i beslutningen. Alle JPG/PNG-filer blir behandlet som oppslag.

2.  **Filnavn**:
    *   Filer som klippes vertikalt får suffiks `(V)` og `(H)`.
    *   Filer som klippes horisontalt får suffiks `(Ø)` og `(N)` (Øvre/Nedre).

### Hvorfor?
*   For å sikre at alle A3-oppslag blir korrekt delt opp, selv om de er skannet sidelengs eller opp-ned, uten at systemet må "gjette" på orienteringen først.
*   Brukeren rapporterte at tidligere logikk feilet på sidelengs skannede ark. Denne løsningen eliminerer feilkilden ved å alltid splitte lengste side.

### Teknisk:
*   Erstattet `splitA3Spread` med `splitImageInHalf` i `fileService.ts`.
*   Oppdatert prosesseringsloopen i `useProjectProcessor.ts` til å kalle denne funksjonen betingelsesløst for alle nye bilder.
