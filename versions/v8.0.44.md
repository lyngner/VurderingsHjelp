
# Oppdatering v8.0.44 - Comprehensive LaTeX Shorthand Cleanup (\t, \r)

Denne oppdateringen fjerner de siste restene av ugyldige LaTeX-kommandoer som `\t` og `\r` som forårsaket "Math input error" i visse løsningsforslag.

### 🛡️ Kritiske endringer:

1.  **Tabular/Text Fix (\t)**:
    *   **Problemet:** KI-en produserte noen ganger `\t` (som betyr "tie-after accent" i LaTeX, eller misforstått tabulator) i stedet for `\text` eller `\mathrm`. Dette ga røde feilmeldinger.
    *   **Løsningen:** `LatexRenderer` reparerer nå `\t` aggressivt:
        *   `\t{...}` -> `\text{...}`
        *   `\tOrd` (Stor forbokstav) -> `\mathrm{Ord}`
        *   Alle andre forekomster av `\t` som **ikke** er starten på en gyldig kommando (som `\text`, `\times`, `\tan`, `\theta`) slettes automatisk.

2.  **Right Fix (\r)**:
    *   **Problemet:** `\r` dukket opp som en feilaktig forkortelse for `\right` eller `\rho`.
    *   **Løsningen:** 
        *   `\r` etterfulgt av parenteser/klammer -> `\right`
        *   Alle andre `\r` som ikke er `\right` eller `\rho` slettes.

3.  **Begin Fix (\b)**:
    *   Utvidet listen over gyldige b-kommandoer til å inkludere `\bullet` og `\beta`.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx` med "Negative Lookahead" regex for å kirurgisk fjerne ugyldige forkortelser uten å ødelegge gyldig kode.
*   Systemversjon: **v8.0.44**.
