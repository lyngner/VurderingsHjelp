
# Oppdatering v8.9.47 - LaTeX Bracket Fix & CAS Newlines

Denne oppdateringen løser kritiske visningsfeil i matematikk (spesielt vektorer) og sørger for at CAS-utregninger får ordentlige linjeskift.

### 🛡️ Kritiske endringer:

1.  **Bracket Argument Error Fix**:
    *   Fikset feilmeldingen `Bracket argument to \\ must be a dimension` som oppstår i MathJax når en linje slutter med `\\` og neste starter med `[` (f.eks. en vektor).
    *   Systemet setter nå automatisk inn en tom gruppe `{}` mellom `\\` og `[` i LaTeX-koden før visning. Dette hindrer at `[...]` feilaktig tolkes som en innstilling for linjeavstand.

2.  **CAS Line Breaks (Explicit BR)**:
    *   Tekst inni figur-boksene ("Tolkning av Gemini") skannes nå for linjeskift (`\n`).
    *   Disse byttes ut med eksplisitte `<br />`-tagger i HTML-en.
    *   Dette sikrer at flerdelt tekst (som CAS-kommandoer) faktisk vises på hver sin linje, selv om nettleserens standardtekstflyt prøver å slå dem sammen.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx` med regex: `.replace(/\\\\(\s*)\[/g, '\\\\$1{} [')`.
*   Oppdatert `LatexRenderer` til å iterere over tekstlinjer og sette inn `<br />`.
*   Oppdatert `VISUAL_MANDATE` i `geminiService.ts` for å kreve doble linjeskift.
*   Systemversjon: **v8.9.47**.
