# Oppdatering v5.5.8 - Interleaved Evidence Flow

Denne oppdateringen løser behovet for pedagogisk sammenheng ved å flytte tekniske bevis (CAS/grafer) ut av isolerte bokser og inn i selve tekststrømmen der eleven faktisk utfører arbeidet.

### 🛡️ Kritiske endringer i v5.5.8:

1.  **Interleaved Context (Regel 22)**:
    *   KI-en er nå instruert til å ikke lenger "dumpe" bevis i et eget felt, men integrere dem direkte i `fullText` ved bruk av tagen `[AI-TOLKNING AV FIGUR: ...]`.
    *   Dette sikrer at læreren ser CAS-utregningen nøyaktig mellom de håndskrevne forklaringene den hører til.

2.  **Strict Terminal Reification**:
    *   CAS-transkriberingen er gjort "rårere" og mer direkte.
    *   Fjernet forklarende tekst som "Linje 1:". Bruker nå et format som ligner mer på GeoGebra/CAS-grensesnittet (f.eks. `$1`, `$2` eller `In/Out`).

3.  **UI-konsolidering**:
    *   I "Kontroll"-modulen er den separate bevis-boksen fjernet fra visningsmodus, da den nå rendres inline av `LatexRenderer`.
    *   Redigeringsmodus har fått et mer strømlinjeformet oppsett for å håndtere interleaved tekst.

4.  **Versjonsstyring**:
    *   Oppdatert `SYSTEM_VERSION` til v5.5.8.
    *   Lagt til Regel 22 (Interleaved Evidence Flow) i `TECHNICAL_STANDARD.md`.

### Teknisk:
*   Oppdatert `geminiService.ts` for å tvinge interleaving i `fullText`.
*   Oppdatert `ReviewStep.tsx` for å fjerne den statiske topp-boksen og forbedre flyten.
*   Justert `LatexRenderer` for enda bedre visning av terminal-liknende CAS-linjer.