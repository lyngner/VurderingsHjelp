
# Oppdatering v9.1.12 - Base Path Config Fix

Denne oppdateringen løser en kritisk konfigurasjonsfeil som forhindret applikasjonen i å laste ("404 Not Found" på index.tsx).

### 🛡️ Kritiske endringer:

1.  **Vite Base Path Correction**:
    *   Endret `base` i `vite.config.ts` fra `'/VurderingsHjelp/'` til `'/'`.
    *   Dette sikrer at applikasjonen laster ressurser korrekt når den kjøres fra roten av et domene (f.eks. på Google Cloud Run), i stedet for å forvente en undermappe.

### Teknisk:
*   Oppdatert `vite.config.ts`.
*   Systemversjon: **v9.1.12**.
