
# Oppdatering v8.1.4 - Deep Scan (Pro Rescan) & Context Fix

Denne oppdateringen løser problemet med "hallusinerte" transkripsjoner (spesielt på digitale CAS-bilder) der Flash-modellen mislykkes totalt.

### 🛡️ Kritiske endringer:

1.  **Deep Scan (Pro Rescan)**:
    *   Når du trykker på **"Transkriber på nytt"** i Kontroll-steget, bruker systemet nå automatisk **Gemini 3 Pro** (i stedet for Flash).
    *   Dette sikrer at vanskelige sider som feilet første gang (hallusinasjoner, feil layout) blir analysert med den smarteste modellen.

2.  **Rubric Context Injection**:
    *   Systemet sender nå en "Cheat Sheet" (oppsummering av oppgavene fra rettemanualen) med i prompten til OCR-modellen.
    *   **Instruks:** *"CONTEXT FROM RUBRIC: Prøven inneholder oppgaver om [Funksjoner, Algebra...]. Bruk dette til orientering, men transkriber KUN elevens tekst."*
    *   Dette hindrer at KI-en "drømmer" om geometri-oppgaver hvis prøven handler om algebra.

3.  **Tvungen Cache-busting**:
    *   Sikret at "Transkriber på nytt" alltid omgår hurtigbufferet og tvinger frem en ny analyse.

### Teknisk:
*   Oppdatert `handleRegeneratePage` i `useProjectProcessor.ts` til å bruke `PRO_MODEL`.
*   Oppdatert `transcribeAndAnalyzeImage` i `geminiService.ts` med `rubricContext` og `modelOverride`.
*   Systemversjon: **v8.1.4**.
