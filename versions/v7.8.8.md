
# Oppdatering v7.8.8 - Visual Wrapper Fix

Denne oppdateringen retter en feil der visuelle bevis (grafikk, CAS) i Word-dokumenter ble vist som tekst i stedet for i det grå "Tolkning av Gemini"-feltet.

### 🛡️ Endringer:

1.  **Regex-utvidelse**:
    *   `LatexRenderer` er oppdatert til å gjenkjenne `[BILDEVEDLEGG X]` og `[BILDEVEDLEGG X: ...]` som gyldige triggere for den grå boksen.
    *   Tidligere var det kun `[AI-TOLKNING AV FIGUR: ...]` som fungerte. Hvis KI-en "glemte" å bytte tag, ble innholdet vist som vanlig tekst.

2.  **Instruksjons-tvang**:
    *   Strammet inn instruksen til Gemini Flash i `analyzeTextContent` for å tvinge beskrivelsen av bildet **inn i klammeparentesene**.
    *   Dette er nødvendig fordi tekst utenfor klammene ikke fanges opp av regex-en som "bevis".

### Teknisk:
*   Oppdatert `SharedUI.tsx` med utvidet regex.
*   Oppdatert `geminiService.ts` med "Visual Wrapper Fix" instruks.
