
# Oppdatering v6.5.8 - Strict Whitelist Enforcement

Denne oppdateringen løser et problem der "hallusinerte" oppgavenavn (f.eks. "44A" i stedet for "4A") slapp gjennom systemets filtre og dukket opp som badges i grensesnittet.

### 🛡️ Kritiske endringer:

1.  **Strict Frontend-Filter**:
    *   Fjernet en logisk svakhet ("loophole") i `useProjectProcessor.ts` som tillot alle oppgaver å passere dersom filter-settet ble oppfattet som tomt.
    *   **Ny regel:** Hvis et prosjekt har en rettemanual (rubric), SKAL alle detekterte oppgaver valideres mot denne. Oppgaver som ikke finnes i manualen (som "44A") blir nå nådeløst fjernet fra metadata, selv om de står i KI-ens svar.

2.  **Display-Backup**:
    *   Lagt inn en ekstra sjekk i `SetupStep.tsx` som skjuler ugyldige oppgaver fra badge-visningen ("Kortene"). Dette fungerer som en siste skanse for visuell ryddighet.

3.  **Forsterket Prompt**:
    *   Oppdatert instruksene til Flash-modellen med konkrete eksempler på hallusinasjoner den skal unngå (f.eks. "4a" vs "44a").

### Teknisk:
*   Refaktorert `integratePageResults` i `hooks/useProjectProcessor.ts`.
*   Oppdatert `geminiService.ts`.
*   Oppdatert `SetupStep.tsx`.
