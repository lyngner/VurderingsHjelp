
# Oppdatering v9.0.7 - LaTeX Repair & Recovery

Denne oppdateringen løser kritiske visningsfeil ("Misplaced &" / "Missing \begin") som skyldes at JSON-parsing ødelegger LaTeX-koder.

### 🛡️ Kritiske endringer:

1.  **Frontend Repair (`\begin` Fix)**:
    *   **Problemet:** JSON-parsing tolker av og til `\b` i `\begin{aligned}` som et kontrolltegn (backspace), noe som gjør at kommandoen blir usynlig eller korrupt (`egin{aligned}`). Dette får MathJax til å krasje.
    *   **Løsningen:** `LatexRenderer` er oppgradert med en "reparasjons-modul" som scanner teksten før visning.
    *   Den gjenoppretter automatisk `\begin{` og `\end{` hvis de er skadet, noe som redder visningen av avansert matematikk umiddelbart.

2.  **Robust Wrapping**:
    *   Sikrer at reparerte `aligned`-miljøer pakkes korrekt inn i `\[ ... \]` for å unngå "Misplaced &"-feil.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx` med nye regex-regler.
*   Systemversjon: **v9.0.7**.
