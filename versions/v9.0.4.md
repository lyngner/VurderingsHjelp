
# Oppdatering v9.0.4 - Throttled Parallelism Fix

Denne oppdateringen løser flaskehalsen der systemet prøvde å analysere alle bildene i et Word-dokument samtidig, noe som førte til timeout før dataene nådde Google.

### 🛡️ Kritiske endringer:

1.  **Throttled Batch Processing (Kø-kontroll)**:
    *   I stedet for å fyre av 20+ API-forespørsler samtidig for et dokument med mange bilder, tar systemet dem nå i puljer på **3 og 3**.
    *   Dette sikrer at hver forespørsel får nok båndbredde og tid til å fullføre uten å bli kvalt av køen i nettleseren.

2.  **Økt Lokal Tidsfrist (180s)**:
    *   Tidsfristen for å lese/pakke ut store Word-filer lokalt er økt fra 60 sekunder til **3 minutter (180s)**. Dette hindrer "Timeout ved lesing av Word-fil" for massive dokumenter.

3.  **Økt Worker Tidsfrist (120s)**:
    *   Hvert enkelt bilde-kall får nå inntil **120 sekunder** på å fullføre, opp fra 60s. Dette gir mer robusthet hvis nettet er tregt.

4.  **Feil-isolering**:
    *   Hvis ett bilde feiler i analysen, krasjer ikke lenger hele dokumentet. Bildet markeres som feilet, mens resten av teksten analyseres som normalt.

### Teknisk:
*   Refaktorert `analyzeTextContent` i `geminiService.ts` for å bruke batch-loop.
*   Oppdatert `fileService.ts` timeout.
*   Systemversjon: **v9.0.4**.
