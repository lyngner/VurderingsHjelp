
# Oppdatering v9.1.5 - Smart Anti-Duplication

Denne oppdateringen løser problemet med at innhold ble vist "dobbelt opp" (både som tekst og i grå boks), og forhindrer at fallback-boksen for visuelle bevis dukker opp unødvendig.

### 🛡️ Kritiske endringer:

1.  **Smart Duplication Check**:
    *   **Problemet:** Hvis KI-en transkriberte tekst fra et bilde direkte i hovedteksten, men *også* la det i `visualEvidence` (uten tag), la systemet automatisk til en grå boks på slutten som en sikkerhetsmekanisme. Dette førte til dubletter.
    *   **Løsningen:** Systemet sjekker nå om innholdet i `visualEvidence` allerede finnes i hovedteksten (ved en smart tekstsammenligning som ignorerer mellomrom).
    *   Hvis teksten er >80% lik, antas det at KI-en har gjort jobben sin "inline", og fallback-boksen undertrykkes.

2.  **Anti-Duplication Mandate**:
    *   Oppdatert systeminstruksene i `geminiService.ts` med en streng regel: **"EITHER use the tag OR write plain text. NEVER BOTH."**
    *   Dette skal hjelpe modellen å velge én strategi i stedet for å helgardere.

### Teknisk:
*   Implementert `isEvidenceAlreadyInText` i `ReviewStep.tsx`.
*   Oppdatert `VISUAL_MANDATE` i `geminiService.ts`.
*   Systemversjon: **v9.1.5**.
