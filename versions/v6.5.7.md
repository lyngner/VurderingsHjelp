
# Oppdatering v6.5.7 - Timeout Guard & Loop Stability

Denne oppdateringen løser kritiske problemer der applikasjonen sluttet å prosessere filer etter 504 Gateway Timeout feil fra Google.

### 🛡️ Endringer i v6.5.7:

1.  **Automatisk Retry på 504/503**:
    *   Implementert en `withRetry` wrapper i `geminiService.ts`.
    *   Systemet prøver nå API-kallet på nytt opptil 3 ganger med økende ventetid hvis det oppstår server-feil (500-serien) eller timeouts.
    *   Dette hindrer at midlertidige nettverksfeil stopper hele prosesseringskøen.

2.  **Robust Loop-logikk**:
    *   Oppdatert `processQueue` i `useProjectProcessor.ts` med en lokal `processedIds` sjekk.
    *   Dette hindrer at loopen "henger" på samme fil hvis React-state oppdateringen er tregere enn loop-hastigheten, noe som tidligere kunne føre til at prosessen stoppet opp tilsynelatende uten grunn.

3.  **Hvorfor dette var nødvendig**:
    *   Bruker rapporterte at prosessen stoppet etter første side og 504-feil i konsollen. V6.5.7 sørger for at systemet tåler disse feilene og fortsetter arbeidet.
