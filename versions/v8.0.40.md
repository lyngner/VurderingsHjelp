
# Oppdatering v8.0.40 - The Cleaner (Aggressive Shorthand Fix)

Denne oppdateringen fjerner irriterende røde feilmeldinger i matematikken ved å implementere en aggressiv "vaskemaskin" i frontend-visningen.

### 🛡️ Kritiske endringer:

1.  **Shorthand Repair (\f, \t, \b)**:
    *   **Problemet:** KI-en produserte noen ganger ugyldige kommandoer som `\f` (ment som `\frac`), `\t` (ment som `\text`) eller isolerte `\b` (støy). Dette ga røde "Math input error" bokser.
    *   **Løsningen:** `LatexRenderer` reparerer nå disse feilene automatisk *før* MathJax ser dem.
        *   `\f{...}` -> `\frac{...}`
        *   `\tOrd` -> `\mathrm{Ord}` (Ser ut som tekst)
        *   `\b` -> Slettes hvis det er støy, eller blir `\begin{aligned}` hvis konteksten stemmer.

2.  **Umiddelbar Effekt**:
    *   Du trenger ikke å skanne sider på nytt for å fikse disse feilene. Koden "helbreder" den eksisterende teksten automatisk når den vises.

3.  **Strict Mandate Update**:
    *   Oppdatert instruksene til KI-en med et eksplisitt forbud mot å bruke disse forkortelsene i fremtiden.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx` med 4 nye regex-regler.
*   Oppdatert `geminiService.ts` med Regel 6 og 7 i `LATEX_MANDATE`.
*   Systemversjon: **v8.0.40**.
