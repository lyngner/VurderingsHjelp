
# Oppdatering v9.1.4 - JSON Escape Fix

Denne oppdateringen løser kritiske feil i parsing av JSON-svar fra KI-en, og finpusser layouten.

### 🛡️ Kritiske endringer:

1.  **Bad Unicode Escape Fix**:
    *   **Problemet:** KI-en returnerte av og til tekst som inneholdt `\u` etterfulgt av ikke-hex-tegn (f.eks. `\union`, `\under`). Dette fikk `JSON.parse` til å krasje med `SyntaxError: Bad Unicode escape`.
    *   **Løsningen:** Systemet kjører nå en regex-vask på JSON-strengen før parsing. Den finner alle `\u` som ikke etterfølges av 4 hex-sifre, og escaper dem automatisk til `\\u`.
    *   Dette gjør at `\union` blir lest som tekststrengen "\union" i stedet for å krasje appen.

2.  **Generell Escape-sikring**:
    *   Lagt til beskyttelse mot ugyldige kontrolltegn som `\a`, `\c` osv. som også er ulovlige i standard JSON.

3.  **Ultra-Tight Layout (CAS)**:
    *   Redusert marginene rundt "Tolkning av Gemini"-boksen til **0**.
    *   Optimalisert padding til `py-1 px-2` for å fjerne den siste resten av "luft" som ble etterlyst.

### Teknisk:
*   Oppdatert `cleanJson` i `geminiService.ts`.
*   Oppdatert styling i `SharedUI.tsx`.
*   Systemversjon: **v9.1.4**.
