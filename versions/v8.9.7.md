
# Oppdatering v8.9.7 - Vector Cleanup & Unicode Repair

Denne oppdateringen løser visuelle problemer i LaTeX-visningen, spesielt rundt vektordiagrammer og spesialtegn.

### 🛡️ Kritiske endringer:

1.  **Vektordiagram-vask (Vector Plot Cleanup)**:
    *   **Problemet:** Diagrammet la automatisk til en ekstra pil `\vec{...}` rundt navnet på vektoren. Hvis navnet allerede var `2\vec{u} - \vec{v}`, ble resultatet en dobbel-pil-suppe som så rotete ut.
    *   **Løsningen:** Systemet stripper nå automatisk vekk LaTeX-kommandoer (`\vec`, `\mathbf`, `{}`) fra navnene i diagrammet før visning.
    *   **Resultat:** Navn vises rent som **"2u - v"** i stedet for kryptisk kode.

2.  **Unicode-reparasjon**:
    *   **Problemet:** Tekster som "m\u00e5 vi f\u00f8rst" (rå unicode) dukket opp i løsningsforslaget.
    *   **Løsningen:** `LatexRenderer` skanner nå etter slike koder og erstatter dem med riktig tegn (`å`, `ø`) før teksten vises.

### Teknisk:
*   Oppdatert `VectorGrid` og `LatexRenderer` i `SharedUI.tsx`.
*   Systemversjon: **v8.9.7**.
