
# Oppdatering v6.6.2 - Visual Evidence Enforcer

Denne oppdateringen sikrer at teknisk innhold (CAS, GeoGebra, Python) alltid havner i den dedikerte grå boksen i stedet for å blandes med elevens håndskrift.

### 🛡️ Kritiske endringer:

1.  **Tvungen Isolering av Teknisk Innhold**:
    *   Systeminstruksene til Gemini Flash er strammet inn. 
    *   **Ny regel:** Alt som ligner på skjermbilder fra PC (CAS, Python, GeoGebra) **SKAL** plasseres i `visualEvidence`-feltet.
    *   Det er nå eksplisitt forbudt å legge dette i hovedteksten, selv om det er lesbar tekst.

2.  **Dynamisk Versjons-tag**:
    *   Den grå boksen ("Tolkning av Gemini") viser nå automatisk gjeldende systemversjon (hentet fra `types.ts`) i stedet for en hardkodet verdi. Dette sikrer at brukeren alltid ser hvilken versjon som har utført tolkningen.

### Teknisk:
*   Oppdatert `geminiService.ts` med strengere instrukser for `visualEvidence`.
*   Oppdatert `SharedUI.tsx` til å importere `SYSTEM_VERSION`.
