
# Oppdatering v8.1.7 - Duplicate Upload Fix & Task Explosion

Denne oppdateringen løser kritiske problemer med filer som "forsvinner" ved opplasting og oppgaver som klumpes sammen i rapportene.

### 🛡️ Kritiske endringer:

1.  **Opphevet Duplikat-sperre**:
    *   **Før:** Systemet nektet deg å laste opp samme fil to ganger ("stille skipping") for å spare ressurser. Dette gjorde at slettede filer ikke kunne lastes opp igjen.
    *   **Nå:** Hvis du laster opp en fil som systemet kjenner igjen, får du lov til det. Filen får automatisk suffikset `(Kopi)` og behandles som ny.

2.  **Tvungen Oppsplitting (Atomic Tasks)**:
    *   Lagt inn en streng instruks til KI-sensoren: **"NEVER group tasks"**.
    *   Dette forbyr sensoren å svare med samlebetegnelser som "1a-d" eller "1ad" i resultattabellen.
    *   Den må nå rapportere hver enkelt deloppgave separat, slik at de matcher rettemanualen og vises korrekt i tabellen.

### Teknisk:
*   Endret `handleCandidateFileSelect` i `useProjectProcessor.ts` til å tillate duplikater med navneendring.
*   Oppdatert `evaluateCandidate` prompt i `geminiService.ts`.
*   Systemversjon: **v8.1.7**.
