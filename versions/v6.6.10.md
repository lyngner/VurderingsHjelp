
# Oppdatering v6.6.10 - Robust Geometry Retry

Denne oppdateringen retter en alvorlig feil i v6.6.9 der bildeanalysen feilet stille, noe som førte til at A3-ark ikke ble rotert eller splittet.

### 🛡️ Kritiske endringer:

1.  **Fjerning av Silent Fail**:
    *   Tidligere versjoner av `detectPageLayout` fanget opp feil og returnerte "0 grader" som default.
    *   Dette førte til at hvis API-kallet feilet (pga. nettverk eller kvote), trodde systemet at bildet var perfekt portrett, og hoppet over både rotasjon og splitting.
    *   **Nå:** Funksjonen kaster en feil, slik at systemets "Retry"-mekanisme kan prøve på nytt.

2.  **Simplified In-Memory Loop**:
    *   Forenklet prosesserings-loopen i `useProjectProcessor` for å unngå komplekse tilstandsoppdateringer som kunne føre til at filer ble "glemt" eller prosessert dobbelt.
    *   Geometri og Transkribering kjøres nå sekvensielt i samme pass (hvis mulig) for å sikre at bildet alltid behandles ferdig.

3.  **Bekreftelse av A3-splitting**:
    *   Med den robuste feilhåndteringen vil systemet nå insistere på å få et gyldig svar fra KI-en før den går videre, noe som sikrer at sidelengs A3-ark alltid blir rotert og klippet.

### Teknisk:
*   Fjernet `try/catch` i `detectPageLayout`.
*   Refaktorert `processQueue` loop.
