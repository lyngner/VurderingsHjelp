
# Oppdatering v9.1.8 - Misplaced & Repair

Denne oppdateringen løser kritiske visningsfeil i matematikkblokker som skyldes JSON-korrupsjon.

### 🛡️ Kritiske endringer:

1.  **Frontend Tag Resurrection**:
    *   `LatexRenderer` er oppgradert til å oppdage og reparere ødelagte LaTeX-tagger.
    *   **Fix:** `\begin` som ble til `\b`+`egin` (eller bare `egin`) blir automatisk gjenopprettet til `\begin`.
    *   **Fix:** `\end` som ble til `\e`+`nd` (eller bare `nd`) blir gjenopprettet til `\end`.
    *   Dette gjør at `aligned`-miljøer som tidligere krasjet med "Misplaced &" nå vises korrekt.

2.  **JSON Guard**:
    *   Oppdatert `cleanJson` til å eksplisitt beskytte `\end`-taggen med dobbel backslash før parsing, for å unngå at den blir tolket som en (ugyldig) escape-kode.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx` med reparasjonslogikk for `egin` og `nd`.
*   Oppdatert `cleanJson` i `geminiService.ts`.
*   Systemversjon: **v9.1.8**.
