
# Oppdatering v7.9.29 - Smart Regex Context Logic

Denne oppdateringen forbedrer systemets evne til å finne oppgaver ("badges") i transkribert tekst som KI-en kanskje har oversett eller formatert uventet.

### 🛡️ Kritiske endringer:

1.  **Format-toleranse**:
    *   Regex-logikken er oppdatert for å håndtere parenteser og mellomrom mer robust.
    *   `1) a)` tolkes nå korrekt som oppgave `1a`. Tidligere krevde systemet `1a` eller `1 a`.

2.  **Kontekstuell Hukommelse (In-Page Memory)**:
    *   Systemet leser nå siden fra topp til bunn og husker hvilket oppgavenummer den er "inne i".
    *   Dette gjør at "ensomme" bokstaver som `b)`, `c)`, `d)` nå automatisk kobles til det sist sette tallet (f.eks. `1b`, `1c`).
    *   Tidligere ble disse ignorert hvis de ikke hadde tallet rett foran seg.

3.  **Whitelist-Filter**:
    *   Alle oppgaver funnet via Regex sjekkes nå mot rettemanualen før de legges til. Dette hindrer at støy i teksten (f.eks. "2x") feilaktig tolkes som oppgave "2x".

### Teknisk:
*   Refaktorert regex-seksjonen i `integratePageResults` (`useProjectProcessor.ts`).
*   Systemversjon: **v7.9.29**.
