
# Oppdatering v7.9.41 - Parent Task Fallback

Denne oppdateringen løser problemet med at detaljerte elevsvar (f.eks. "2a", "2b") ble avvist eller filtrert bort hvis rettemanualen kun inneholdt hovedoppgaven (f.eks. "Oppgave 2").

### 🛡️ Kritiske endringer:

1.  **Parent Mapping Logic (Fallback)**:
    *   Systemet har nå en smartere "Whitelist"-sjekk.
    *   Hvis KI-en finner en deloppgave (f.eks. "2A") som **ikke** finnes i rettemanualen, sjekker systemet om **hovedtallet** ("2") finnes.
    *   Hvis hovedtallet finnes, blir oppgaven automatisk omgjort til hovedoppgaven. "2A" blir til "2".
    *   Dette gjør at læreren kan ha en enkel rettemanual ("Oppgave 1, 2, 3") selv om elevene svarer på "1a, 1b, 2a, 2b".

2.  **Utvidet Filter-visning**:
    *   Økt høyden på oppgavefilteret i Kontroll-panelet (ReviewStep) for å gi plass til flere badger uten skrolling.

### Teknisk:
*   Oppdatert `integratePageResults` i `useProjectProcessor.ts` med `flatMap` og fallback-logikk.
*   Justert CSS i `ReviewStep.tsx`.
*   Systemversjon: **v7.9.41**.
