
# Oppdatering v8.2.3 - MathJax Argument Error Fix

Denne oppdateringen løser en spesifikk visningsfeil: `Could not find closing ']' for argument to \\`.

### 🛡️ Kritiske feilrettinger:

1.  **Safety Guard for `\\[`**:
    *   **Problemet:** I v8.2.2 ble regexen som reparerer mellomrom (`\ [` -> `\[`) litt for ivrig. Den kunne feilaktig matche `\\ [` (linjeskift + mellomrom + parentes), og gjøre det om til `\\[` (linjeskift med argument).
    *   Hvis denne argument-parentesen ikke ble lukket riktig (fordi det egentlig ikke var et argument), krasjet MathJax.
    *   **Løsningen:** Regexen er oppdatert med en "negative lookbehind"-sjekk (`(^|[^\\])`) for å sikre at den ALDRI rører `\\` (dobbel backslash).

2.  **Robusthet**:
    *   Dette sikrer at linjeskift i LaTeX (`\\`) fungerer som de skal, selv om neste linje starter med `[` eller `(`.

### Teknisk:
*   Oppdatert `sanitizeLatex` i `SharedUI.tsx`.
*   Systemversjon: **v8.2.3**.
