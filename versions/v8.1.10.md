
# Oppdatering v8.1.10 - CSV Export

Denne oppdateringen legger til en etterspurt funksjon for å eksportere resultatdata til eksterne verktøy.

### 📊 Ny funksjon:
*   **Eksporter til Regneark (CSV)**:
    *   Lagt til en knapp under resultattabellen som laster ned en CSV-fil.
    *   Filen inneholder alle kolonner fra matrisen: Kandidatnavn, poeng per oppgave, totalsum, prosent og karakter.
    *   Formatert med semikolon (`;`) som skilletegn for å fungere sømløst med norsk Excel (som bruker komma som desimalskilletegn).
    *   Inkluderer UTF-8 BOM for korrekt visning av norske tegn (æøå).

### Teknisk:
*   Implementert `handleExportCSV` i `ResultsStep.tsx`.
*   Systemversjon: **v8.1.10**.
