
# Oppdatering v7.9.0 - Sibling ID Inference

Denne oppdateringen løser problemet med at A3-ark som er splittet i to ofte ender opp med en "Ukjent" side hvis kandidatnummeret kun er skrevet på den ene halvdelen av arket.

### 🛡️ Endringer:

1.  **Søsken-utledning (Sibling Inference)**:
    *   Når en side analyseres, sjekker systemet nå filnavnet (uten (V)/(H) suffiks).
    *   **Før:** Hvis siden manglet ID, ble den markert som "Ukjent".
    *   **Nå:** Hvis siden mangler ID, sjekker systemet om det finnes en annen side (et "søsken") fra samme originalfil som *har* en ID. Hvis ja, adopterer siden denne ID-en automatisk.

2.  **Redningsaksjon (Rescue)**:
    *   Fungerer også "bakover". Hvis systemet først prosesserer en side uten ID (blir Ukjent), og deretter prosesserer "søskenet" som *har* ID, vil systemet automatisk hente den ukjente siden og flytte den til riktig kandidat.

3.  **Resultat**:
    *   Drastisk reduksjon i antall "Ukjente kandidater" ved skanning av A3-bunker.

### Teknisk:
*   Utvidet `integratePageResults` i `hooks/useProjectProcessor.ts` med logikk for filnavn-matching.
*   Benytter `deleteCandidate` fra `storageService` for å rydde opp i tomme ukjente kandidater etter sammenslåing.
