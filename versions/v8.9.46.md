
# Oppdatering v8.9.46 - MathJax i CAS-boks

Denne oppdateringen sikrer at matematikk som transkriberes i CAS-felt ("Tolkning av Gemini") vises som formatert matematikk i stedet for ren tekst.

### 🛡️ Kritiske endringer:

1.  **Frontend Rendering Fix**:
    *   Tekst inni `[BILDEVEDLEGG: ...]`-boksene blir nå merket for MathJax-prosessering.
    *   Tidligere ble innholdet her behandlet som ren tekst, slik at `\( x^2 \)` ble vist som koden `\( x^2 \)`. Nå vises det som $x^2$.

2.  **Strict Newline Mandate**:
    *   KI-en har fått eksplisitt beskjed om å bruke **doble linjeskift** (`\n\n`) mellom linjer i CAS-rekonstruksjoner.
    *   Dette sikrer at hver kommandolinje havner på en ny linje visuelt, noe som gjør det lettere å lese.

### Teknisk:
*   Oppdatert `LatexRenderer` i `SharedUI.tsx` til å legge til `tex2jax_process` på nestede elementer.
*   Oppdatert `VISUAL_MANDATE` i `geminiService.ts`.
*   Systemversjon: **v8.9.46**.
