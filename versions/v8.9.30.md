
# Oppdatering v8.9.30 - Rename Fix

Denne oppdateringen løser en kritisk feil der endring av prosjektnavn i oversikten ikke ble lagret.

### 🛡️ Endringer:

1.  **Sikker Metadata-oppdatering**:
    *   **Problemet:** Når man endret navn på et prosjekt fra Dashboardet, sendte appen et "grunt" prosjekt-objekt (uten kandidatliste) til lagringsfunksjonen.
    *   **Krasjet:** Lagringsfunksjonen prøvde å lese `project.candidates.map(...)` for å rydde opp i foreldreløse filer. Siden listen manglet, krasjet koden før navnet ble lagret.
    *   **Løsningen:** Lagringsfunksjonen (`saveProject`) sjekker nå om kandidatlisten faktisk eksisterer før den prøver å rydde eller telle kandidater. Hvis listen mangler (som ved navnebytte), oppdateres kun metadata (navn/dato), mens resten av databasen forblir urørt.

2.  **Statistikk-bevaring**:
    *   Sikret at `candidateCount` og `evaluatedCount` bevares ved navnebytte, i stedet for å nullstilles.

### Teknisk:
*   Oppdatert `saveProject` i `services/storageService.ts` med null-sjekker på `project.candidates`.
*   Systemversjon: **v8.9.30**.
