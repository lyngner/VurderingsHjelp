
# Oppdatering v8.9.44 - Auto-Gen UI Cleanup

Denne oppdateringen rydder opp i "Knapp vs Auto"-logikken.

### 🛡️ Endringer i v8.9.44:

1.  **Fjerning av "Generer Fasit"-knappen**:
    *   Knappen er nå bekreftet fjernet fra brukergrensesnittet i `SetupStep`.
    *   Statusboksen for rettemanual er gjort smartere: Den vises nå alltid hvis du har lastet opp oppgaver, og sier "**Venter på generering...**" hvis systemet tenker, eller "**Genererer rettemanual...**" når den jobber.

2.  **Robust Auto-Trigger**:
    *   Flyttet logikken for automatisk generering inn i selve prosessorkjernen (`useProjectProcessor.ts`).
    *   Dette sikrer at prosessen starter umiddelbart når oppgavefilene er klare (`processingCount === 0`), uten at du trenger å trykke på noe.

3.  **Fallback**:
    *   Hvis den automatiske starten mot formodning skulle feile (f.eks. pga. nettverkshikk), kan du nå klikke på statusboksen ("Venter på generering...") for å tvinge i gang prosessen manuelt. Dette er en skjult sikkerhetsventil.

### Teknisk:
*   Refaktorert `useProjectProcessor.ts` for å inkludere `auto-trigger` useEffect.
*   Ryddet opp i `SetupStep.tsx`.
*   Systemversjon: **v8.9.44**.
