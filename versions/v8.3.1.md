
# Oppdatering v8.3.1 - Line Breaks & Queue Lock

Denne oppdateringen sikrer korrekt formatering i rettemanualen og strammer inn på rekkefølgen i prosesseringskøen.

### 🛡️ Kritiske endringer:

1.  **Queue Lock (Ferdig Fasit)**:
    *   Systemet venter nå til rettemanualen er *helt ferdig* før den begynner å transkribere elevsvar.
    *   Hvis du laster opp elevsvar *mens* fasiten genereres, vil systemet klargjøre bildene (geometri/splitting), men pause selve analysen til fasiten er klar. Dette hindrer feilmatching mot uferdig manual.

2.  **Retteveiledning Linjeskift**:
    *   Lagt inn en automatisk formaterer som tvinger hvert punkt i `commonErrors` (f.eks. `[-0.5 p]`) ned på en ny linje.
    *   Dette gjør veiledningen mye mer lettlest i tabeller og oversikter.

### Teknisk:
*   Oppdatert `geminiService.ts` med `formatCommonErrors`.
*   Oppdatert `useProjectProcessor.ts` med blocking-logikk.
*   Systemversjon: **v8.3.1**.
