
# Oppdatering v9.1.3 - Arrow & Spacing Fix

Denne oppdateringen løser feilen der `\rightarrow` ble vist som `ightarrow` og fjerner ytterligere luft i CAS-boksene.

### 🛡️ Kritiske endringer:

1.  **Arrow-Fix (`ightarrow`)**:
    *   **Problemet:** En sikkerhetsfunksjon som skulle fjerne `\r` (vognretur) i tekst var for aggressiv og slettet også `\r` i `\rightarrow`.
    *   **Løsningen:** Regelen er oppdatert til å *tillate* `\r` hvis det etterfølges av `ight` (som i `\right...`) eller `a-z` (som i de fleste kommandoer). Dette gjenoppretter pilene i matematikken umiddelbart.

2.  **Ultra-Tight CAS Layout**:
    *   Fjernet doble linjeskift *inni* figur-boksene. Alle tomme linjer kollapses nå automatisk.
    *   Redusert marginene rundt boksen til det absolutte minimum (2px).

3.  **Auto-Math for Arrows**:
    *   Oppdatert "Auto-Math Wrapper" til å gjenkjenne piler (`arrow`) som matematikk, slik at linjer som `\rightarrow 25` automatisk pakkes inn i `\( ... \)` og vises korrekt.

### Teknisk:
*   Oppdatert `sanitizeLatex` regex i `SharedUI.tsx`.
*   Systemversjon: **v9.1.3**.
