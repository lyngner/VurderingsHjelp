
/**
 * Google Drive integrasjon deaktivert v4.19.0
 * Alle filer må nå lastes opp lokalt for bedre personvern og lokal arbeidsflyt.
 */
export const extractFolderId = (url: string): string | null => null;
export const fetchImagesFromDriveFolder = async (folderId: string): Promise<any[]> => [];
export const downloadDriveFile = async (fileId: string): Promise<any> => { throw new Error("Drive-integrasjon er deaktivert."); };
